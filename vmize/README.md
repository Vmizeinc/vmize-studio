# Vmize

This repository contains the Vmize Studio project.

Structure:

- `.github/workflows/` — CI/CD workflows
- `backend/` — Express backend with routes, services, and scripts
- `frontend/` — Static frontend site used for marketing and admin UI
- `docs/` — Project documentation

See `backend/README.md` for backend setup and `frontend/` files for frontend usage.