# Deployment

This repository includes recommended deployment instructions for the frontend and backend. See `backend/DEPLOYMENT.md` for detailed backend deployment steps (Docker, VPS, GitHub Actions), and the top-level workflows in `.github/workflows/` for CI examples.

(For a complete step-by-step guide, see `backend/DEPLOYMENT.md`.)