# Analytics

This project includes simple analytics tracking in `backend/analytics-tracker.js` and analytics endpoints when the proxy-with-analytics server is used.

- `GET /api/analytics` — Summary
- `GET /api/analytics/funnel` — Conversion funnel
- `POST /api/track` — Track custom event

Add more documentation about metrics and dashboards here.